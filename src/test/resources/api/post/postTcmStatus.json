{
   "items":[
      {
         "testCaseIds": ${testCaseIds},
         "status":{
            "id":"${statusId}"
         },
         "executionType": "AUTOMATED",
         "details":"${api_url}/projects/${project_id}/automation-launches/${automationLaunchId}/tests/${automationExecutionId}"
      }
   ]
}